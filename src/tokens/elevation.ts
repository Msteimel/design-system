// Elevation. A single shadow step for phase 3's `Card` `elevated` state.
// Not light/dark specific (same rule tasty-tomes/personal-compass use for
// radius, spacing, and font-size — see adapters/css.ts).
export const shadow = '0 4px 12px rgba(0, 0, 0, 0.15)' as const
